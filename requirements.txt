# Core runtime dependencies
python-nmap>=0.7.1
requests>=2.31.0
rich>=13.0.0
jinja2>=3.1.0
python-dotenv>=1.0.0
urllib3>=2.0.0

# Development / testing (install with: pip install -r requirements-dev.txt)
# These are split into requirements-dev.txt to keep the main file lean
